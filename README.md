
  # Store Audit Dashboard

  This is a code bundle for Store Audit Dashboard. The original project is available at https://www.figma.com/design/ovuUWMywC1MUr2IkEvhGwl/Store-Audit-Dashboard.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  